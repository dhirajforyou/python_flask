# python_flask
Flask experiments


db commands:
 - create migration repo
    flask db init
 - migration
    flask db migrate -m "Initial migration"
 - upgrade db
    flask db upgrade

test:
 - python -m unittest -v
